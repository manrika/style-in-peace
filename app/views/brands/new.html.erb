<%= render 'shared/navbar' %>
 <%= simple_form_for(@brand) do |f| %>
  <%= f.input :name %>
  <%= f.input :style%>
  <%= f.input :website_url %>
  <%= f.submit "Create outfit", class: "btn" %>
 <% end %>

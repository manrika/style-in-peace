<%= render 'shared/navbar' %>

<div class="mt-5">
  <div class="page-layout-with-back-btn">
    <div class="back-btn-container">
      <%= link_to 'javascript:history.go(-1);' do %>
        <button class="icon-btn">
          <i class="fas fa-chevron-left"></i>
        </button>
      <% end %>
    </div>

    <div class="container">
      <h1>Saved brands</h1>
      <div class="d-flex flex-wrap" data-search-brand-cards-target="cards">
        <% @saved_brands.each do |sb| %>
          <div data-controller="brand-card col-3">
            <div data-action="mouseenter->brand-card#changeCard mouseleave->brand-card#changeCardBack">
              <div data-brand-card-target="card">
                <%= render 'brands/simple_card', brand: sb.brand %>
              </div>
              <div class='d-none' style="width: 300px; height: auto;" data-brand-card-target="info">
                <%= render 'brands/info_card', brand: sb.brand  %>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>

<%= render 'shared/navbar' %>

<div class="mt-5">
  <div class="page-layout-with-back-btn">
    <div class="back-btn-container">
      <%= link_to 'javascript:history.go(-1);' do %>
        <button class="icon-btn">
          <i class="fas fa-chevron-left"></i>
        </button>
      <% end %>
    </div>

    <div class="container" data-controller="notification">
      <h1>Explore eco-brands</h1>

      <!-- Modal -->
      <div class="modal fade" id="suggestBrandModal" tabindex="-1" aria-labelledby="suggestBrandModalLabel" aria-hidden="true">
        <div class="modal-dialog">
          <div class="modal-content">
            <div class="modal-header">
              <h5>Suggest a brand</h5>
              <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                <span aria-hidden="true">&times;</span>
              </button>
            </div>
            <div class="modal-body">
              <%= simple_form_for(@brand, data: {action: "submit->notification#displaySuccessNotif"}) do |f| %>
                <%= f.input :name, label: "Brand name" %>
                <%= f.input :website_url, label: "Website"%>
                <%= f.input :style, collection: %w[modern outdoor minimalist retro boujie arty scandinavian grunge formal lounge boho] %>
                <%# <button class="simple-text-btn" data-toggle="modal" data-target="#successModal"> %>
                  <%= f.submit "Send brand suggestion", class: "submit-btn" %>
                <%# </button> %>
              <% end %>
            </div>
          </div>
        </div>
      </div>

      <% if current_user.brands_referred == nil || current_user.brands_referred == 0 %>
        <div class="explore-notif">
            <div class="message">
              <p><strong>Heeey <%= current_user.first_name.capitalize %></strong><br>We are always looking to list new eco-firendly brands. If you know any eco-friendly brands let us know and boost your impact!</p>
            </div>
          <div class="buttons">
            <button class="exit-btn"><i class="fas fa-times"></i></button>
              <button class="simple-text-btn" data-toggle="modal" data-target="#suggestBrandModal">
                Suggest brand <i class="fas fa-arrow-right"></i>
              </button>
          </div>
        </div>
      <% end %>

      <div class="explore-notif d-none" id="success-notif" data-notification-target="success">
        <div class="message">
          <p><strong>Thank you</strong><br>A member of the Style in Peace team will look into the impact of your brand suggestion soon!</p>
        </div>
        <div class="buttons">
          <button class="exit-btn"><i class="fas fa-times"></i></button>
        </div>
      </div>

      <div class="mytabs">
        <input type="radio" id="tabfree" name="mytabs" checked="checked">
        <label for="tabfree">Brands</label>
        <div class="tab p-0 search" data-controller="search-brand-cards">
          <%= render 'search_ecobrand' %>
          <%= render 'brands_list', brands: @brands %>
        </div>
        <input type="radio" id="tabsilver" name="mytabs">
        <label for="tabsilver">News</label>
        <div class="tab p-0 search" data-controller="search-news">
          <%= render 'search_news' %>
          <%= render 'news_list', newsarticles: @newsarticles %>
      </div>
    </div>
  </div>
</div>


<!-- Modal -->
      <%# <div class="modal fade" id="successModal" tabindex="-1" aria-labelledby="successModalLabel" aria-hidden="true">
        <div class="modal-dialog">
          <div class="modal-content">
            <div class="modal-header">
              <h5>Thanks for sending a brand!</h5>
              <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                <span aria-hidden="true">&times;</span>
              </button>
            </div>
            <div class="modal-body">
              <p>A member of the Style in Peace team will check out the impact of this brand!</p>
            </div>
          </div>
        </div>
      </div> %>

<div class='container'>
  <h2>How eco-friendly are the brands you shop with?</h2>

<%= render 'search' %>

  <div>
    <h2>New eco-brands on the block</h2>
    <div class='d-flex flex-wrap justify-content-center'>
      <% @new_brands.first(10).each do |brand| %>
        <div class="TODO-CARDS-scss w-25 m-2">
          <img class = "img-thumbnail" src="<%= brand.splash_image %>" alt="">
          <p><strong><%= brand.name %></strong></p>
          <p> <%= '🌎' * brand.average_rating %></p>
          <p> <%= '£' * brand.price_category %></p>
          <p> <em><%= brand.style %></em></p>
          <%= link_to 'view', ecofriendly_brand_path(brand) %>
        </div>
      <% end %>
    </div>
  </div>

  <% if current_user %>
    <div>
      <h2>Eco-brands for you</h2>
      <div class="d-flex flex-wrap justify-content-center">
        <% @recommended_brands.first(10).each do |brand| %>
          <div class="TODO-CARD-scss w-25 m-2">
            <img class = "img-thumbnail" src="<%= brand.splash_image %>" alt="">
            <p><strong><%= brand.name %></strong></p>
            <p><%= '🌎' * brand.average_rating %></p>
            <p> <%= '£' * brand.price_category %></p>
            <p> <em><%= brand.style %></em></p>
            <%= link_to 'view', ecofriendly_brand_path(brand) %>
          </div>
        <% end %>
      </div>
    </div>
  <% end %>
</div>





<p>TODO: Footer</p>
